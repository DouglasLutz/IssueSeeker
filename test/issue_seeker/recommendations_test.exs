defmodule IssueSeeker.RecommendationsTest do
  use IssueSeeker.DataCase

  describe "filter_projects_for_profile/1" do
    test "with a profile returns the matching projects" do
      # TODO
      assert true
    end
  end

  describe "create_recommendation/1" do
    test "with a user creates a recommendation for the user" do
      # TODO
      assert true
    end
  end

  describe "get_user_recommendations/1" do
    test "with a user the associated recommendations" do
      # TODO
      assert true
    end
  end

  describe "get_recommendations_issues_from_recommendation_id/1" do
    test "with a valid recommendation id returns the associated issues" do
      # TODO
      assert true
    end
  end

  describe "get_recommendation_issue/1" do
    test "with a valid id returns the RecommendationIssue" do
      # TODO
      assert true
    end

    test "with a nonexistend id returns nil" do
      # TODO
      assert true
    end
  end
end
