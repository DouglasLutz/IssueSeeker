defmodule IssueSeeker.ContributorTest do
  use IssueSeeker.DataCase

  describe "changeset/2" do
    test "casts the allowed fields" do
      #TODO
      assert true
    end

    test "validates the required fields" do
      #TODO
      assert true
    end

    test "validates the unique constraints" do
      #TODO
      assert true
    end
  end
end
