defmodule IssueSeekerWeb.LabelControllerTest do
  use IssueSeekerWeb.ConnCase

  describe "index" do
    test "lists all labels", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "issues" do
    test "lists all issues associated with the label", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "edit" do
    test "renders form for editing chosen label", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "update" do
    test "redirects when data is valid", %{conn: _conn} do
      # TODO
      assert true
    end
  end
end
