defmodule IssueSeekerWeb.IssueControllerTest do
  use IssueSeekerWeb.ConnCase

  describe "index" do
    test "with project id lists all associated open issues", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "show" do
    test "renders issue details", %{conn: _conn} do
      # TODO
      assert true
    end
  end
end
