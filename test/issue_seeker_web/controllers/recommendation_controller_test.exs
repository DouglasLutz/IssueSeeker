defmodule IssueSeekerWeb.RecommendationControllerTest do
  use IssueSeekerWeb.ConnCase

  describe "index" do
    test "lists all requested recommendations", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "projects" do
    test "lists all projects matching the user's profile", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "create" do
    test "generates new recommendation and redirects", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "show" do
    test "renders recommendation issues", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "show_issue" do
    test "renders issue details", %{conn: _conn} do
      # TODO
      assert true
    end
  end
end
