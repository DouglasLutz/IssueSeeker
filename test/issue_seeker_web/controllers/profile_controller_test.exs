defmodule IssueSeekerWeb.ProfileControllerTest do
  use IssueSeekerWeb.ConnCase

  describe "new" do
    test "renders form", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "create" do
    test "redirects when data is valid", %{conn: _conn} do
      # TODO
      assert true
    end

    test "renders errors when data is invalid", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "edit" do
    test "renders form for editing", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "update" do
    test "redirects when data is valid", %{conn: _conn} do
      # TODO
      assert true
    end

    test "renders errors when data is invalid", %{conn: _conn} do
      # TODO
      assert true
    end
  end

  describe "show" do
    test "renders profile details", %{conn: _conn} do
      # TODO
      assert true
    end
  end
end
